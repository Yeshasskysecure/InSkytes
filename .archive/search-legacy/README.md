Legacy client-side search fallback archive.

These snippets were removed from the active SPFx search page so the UI cannot
fall back to SharePoint Search plus browser-side file download/parsing.

Active search/chat now requires the Knowledge Search API backend.
